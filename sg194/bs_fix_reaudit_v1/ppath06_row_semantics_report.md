# PPATH06 Row Semantics Report

- Publication path id: `PPATH06`.
- Residual-support rows: `[22, 23, 24]`.
- Overall interpretation: Publication rows 22/23/24 are the three phase-aware endpoint-class balance constraints on PPATH06. Each row is a pure pair-difference relation on P3 multiplicities, inherited directly from raw L2 through the internal FPATH07 shell without any new publication-only deformation.

- row `22` / basis `L2_phase_aware_class_01` / row-kind `phase_aware_endpoint_class` / raw source `L2` / terms `[{'unknown': 'P3_R1', 'coeff': 1}, {'unknown': 'P3_R4', 'coeff': -1}]`: Pair-difference constraint enforcing equal multiplicities of `P3_R1` and `P3_R4` at endpoint `P3` along the publication path `PPATH06`.
- row `23` / basis `L2_phase_aware_class_02` / row-kind `phase_aware_endpoint_class` / raw source `L2` / terms `[{'unknown': 'P3_R2', 'coeff': 1}, {'unknown': 'P3_R3', 'coeff': -1}]`: Pair-difference constraint enforcing equal multiplicities of `P3_R2` and `P3_R3` at endpoint `P3` along the publication path `PPATH06`.
- row `24` / basis `L2_phase_aware_class_03` / row-kind `phase_aware_endpoint_class` / raw source `L2` / terms `[{'unknown': 'P3_R5', 'coeff': 1}, {'unknown': 'P3_R6', 'coeff': -1}]`: Pair-difference constraint enforcing equal multiplicities of `P3_R5` and `P3_R6` at endpoint `P3` along the publication path `PPATH06`.
