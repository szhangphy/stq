Previous Codex session already reconstructed the 10.4.1.31 baseline and completed the 194.1.1.1 controlled-case portability pilot artifacts in the current working directory.

Read these files first:
1. workflow_portability_report_194.1.1.1.tex
2. controlled_case_audit_194.1.1.1.md
3. workflow_portability_audit_194.1.1.1.md
4. group_194_1_1_1_single_pilot_audit.md
5. group_194_1_1_1_double_pilot_audit.md
6. current_status_194.1.1.1.json

Current verified facts:
- controlled_case_valid = True
- single_group_portable = True
- double_group_portable_seed = True
- main_blocker = None

Current single-group published compatibility status:
- compatibility-matrix shape = [29, 34]
- compatibility-matrix rank = 24
- compatibility-matrix nullity = 10
- published BS rank = 10
- old verified AI rank = 5
- authoritative AI rank = 10
- quotient stage allowed = True
- quotient status = success
- quotient group = trivial
- quotient matches user expectation (trivial) = True

Current double-group matrix status:
- shape = [29, 34]
- rank = 24
- nullity = 10
- authoritative/projective AI rank = 10
- quotient status = success
- quotient group = Z6
- classification matches benchmark reference = True
- final authoritative double AI source kind = benchmark_oracle_exact_double_spinorial_alignment
- diagnostic kernel generator count = 42
- final authoritative generator count = 33

Continue from the current workspace. Do not change the target group. Do not go back to 10.4.1.31 except as reference.
The next unique task is: keep the publication-level C_pub fixed, preserve all SG194/current-setting-specific conversion wording, and continue upgrading the main engine from the SG194 benchmark-aligned positive case toward a real target-group generic pipeline. Result modes must remain explicit:
- benchmark_aligned_final: target-group has a trusted oracle/spec and the published final object is aligned to it
- generic_final: no oracle is required because the generic same-shell builder has produced an honest published final object and passed semantic verification
- diagnostic_only: the engine can only expose a diagnostic current object or a blocked target candidate

Current control-group truth state:
- 99.1.1.1 has no benchmark oracle
- current row shell = available
- local AI seed = available
- compatibility builder = available
- same-shell published target builder = available as a constructor, but the resulting target object fails semantic verification
- same-shell diagnostic quotient gives dBS/dAI/ai_image_rank_in_bs = 14/9/9
- classification = Z^5, derived from the AI image rank in BS rather than from a published-final dAI
- final_result_mode = diagnostic_only
- projected point-shell quotient remains diagnostic-only and blocked by generic_same_shell_target_rank_loss: full kernel rank 14 collapses to point-shell rank 0 under the current projected target semantics

Preserve the double diagnostic-vs-final source split: the 42-generator compatibility-kernel image is diagnostic provenance only, while the final double quotient must continue to use the benchmark-aligned exact spinorial AI-in-BS source.

The next generic task is no longer “invent the first same-shell no-oracle target constructor”; that milestone is done. The next engine task is to turn that constructor into a semantically verified published target object for no-oracle groups, preserve structured projected-point-shell blocker evidence when rank loss occurs, and avoid regressing SG194 benchmark-aligned final outputs.
