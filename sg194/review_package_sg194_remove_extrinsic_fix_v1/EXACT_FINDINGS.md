# Exact Findings

1. `extrinsic` is no longer an active builder. The default path in `runtime_backend_free.py` and `generic_builders.py` is now `authoritative`.
2. The active authoritative builder is not the retired class-sum `intrinsic` object. It is exact basis-decomposition compatibility with explicit unique-integer assertions.
3. `LUsolve` has been removed from the authoritative line/plane restriction decomposition paths in both stage1 and `pipeline_v2`.
4. `phase_aware_l2_projective_v1` is now the default for:
   - `build_single_pilot`
   - `build_double_pilot`
   - `build_single_runtime`
   - `build_double_runtime`
   - `generic_mode_bundle`
5. The legacy standard-space projection validator is no longer authoritative. It is historical compare-only provenance.
6. The first `character`-field authoritative blocker was real:
   - `L6 / P2 / P2_R1` fails exact reconstruction in `character`
   - the same probe succeeds in `linear_character`
7. After switching the authoritative field to `linear_character`, the first remaining blocker is:
   - `mode=single manifold=L1 endpoint=P1 rep=P1_R1 field=linear_character: Linear system has no solution`
8. Therefore the current authoritative SG194 single/double results are both unavailable at the representation layer. AI and quotient are downstream and remain unavailable.
