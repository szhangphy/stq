# Review Map

- `source_layer/`
  Active source edits for the unique authoritative builder, stage1/stage2 wiring, and legacy projection retirement.

- `audit_layer/`
  New round outputs: patch summary, authoritative regression, solver audit, stage2 role cleanup, authoritative single/double results, consistency check.

- `compare_layer/`
  Historical compare material used to explain why the old coarse `13` and retired class-sum builders are not authoritative.

- `run_layer/`
  Commands, key terminal output, git status, diff stat, commit SHA, push status.

- `semantics_layer/`
  Current status, handoff, next-step prompt, and an explicit semantics boundary note.

- `deliverable_layer/`
  The ten highest-signal files copied into one place.
