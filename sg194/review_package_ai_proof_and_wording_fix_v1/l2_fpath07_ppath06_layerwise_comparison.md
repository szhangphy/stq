# L2 / FPATH07 / PPATH06 Layerwise Comparison

- Chain: `L2` -> `FPATH07` -> `PPATH06`.
- Mapping is index-shift only: `True`.

- raw/internal/publication rows `6` / `23` / `19` share basis `CANDIDATE_PATH_003_R1` and identical matrix rows = `True`.
- raw/internal/publication rows `7` / `24` / `20` share basis `CANDIDATE_PATH_003_R2` and identical matrix rows = `True`.
- raw/internal/publication rows `8` / `25` / `21` share basis `CANDIDATE_PATH_003_R3` and identical matrix rows = `True`.
- raw/internal/publication rows `9` / `26` / `22` share basis `L2_phase_aware_class_01` and identical matrix rows = `True`.
- raw/internal/publication rows `10` / `27` / `23` share basis `L2_phase_aware_class_02` and identical matrix rows = `True`.
- raw/internal/publication rows `11` / `28` / `24` share basis `L2_phase_aware_class_03` and identical matrix rows = `True`.
