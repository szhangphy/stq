# Exact Findings

1. Q1: `13` intrinsic 也能得到吗？single/double = `7` / `7`；extrinsic = `1` / `1`。
2. Q2: 去掉 extrinsic fingerprints 后，`L3` row counts = single `9`、double `9`；extrinsic 则是 single `16`、double `16`。
3. Q3: `L2` 在 intrinsic 下的 row counts = single `5`、double `5`。
4. Q4: 只被 extrinsic 杀掉的 kernel 方向维数 = single `6`、double `6`。
5. Q5: 只在 extrinsic 下被拒的 AI candidates = single `[]`、double `[]`。
6. intrinsic 推荐结果：single `dBS=7, final_dAI=None, surviving_ai_rank=0`；double `dBS=7, final_dAI=None, surviving_ai_rank=0`。
