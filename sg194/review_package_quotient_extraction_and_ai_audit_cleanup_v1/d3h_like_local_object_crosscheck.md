# D3h-like Local Object Crosscheck

- Site-symmetry type: `D3h_like`.
- Families: `['b', 'c', 'd']`.
- Same stabilizer ordering across b/c/d: `True`.
- Same character vectors across b/c/d: `True`.
- Verdict: No family-level local-library mismatch is visible for D3h_like. Families b/c/d share the same stabilizer ordering and the same unitary character vectors for each audited local object label.

- `A1'`: same ordering = `True`, same character vector = `True`.
- `A1''`: same ordering = `True`, same character vector = `True`.
- `A2'`: same ordering = `True`, same character vector = `True`.
- `A2''`: same ordering = `True`, same character vector = `True`.
- `E'`: same ordering = `True`, same character vector = `True`.
- `E''`: same ordering = `True`, same character vector = `True`.
