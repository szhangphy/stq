# Single-Group Magnetic Local Corep Audit for 10.4.1.31

## Scope

- Group only: `10.4.1.31`.
- This round only handles the point-like antiunitary families `a, b, d, e, g, h`.
- The trusted k-space side, plane formalism, and real-space / k-space bridge are reused as fixed input.
- The parameter families `n, i, j` remain out of scope in this script.

## Processing Order

1. `e`, `d`: same `2'/m'` site symmetry, easiest inversion-based unitary subgroup, and the first pair that actually probes the old rank-2 gap.
2. `h`, `g`: same `2/m'` site symmetry, same case-a logic, but lower priority than `e/d` because they do not add new rank directions first.
3. `b`, `a`: same `2'/m` site symmetry, still easy to build, but lowest marginal value because `a` duplicates the corresponding `b` vectors in the present unitary k-space target.

## Toolchain Feasibility

- `swyckoff_r.py` and the audited bridge are sufficient to recompute the point stabilizers exactly for all six target families.
- `SSGReps/SSGReps/SSGReps.py` contains antiunitary Wigner/corep logic for little groups, but it does not provide a direct helper that consumes an arbitrary real-space site stabilizer as a local magnetic corep group.
- A dedicated single-group builder is therefore used here: enumerate the order-2 unitary subgroup irreps, inspect the antiunitary action on that subgroup, classify the Wigner case, and build the corresponding magnetic local coreps explicitly.

## Family-By-Family Audit

### Family `e`

- Representative coordinate: `1/2, 1/4, 0`.
- Multiplicity: `4`.
- Site symmetry: `2'/m'`.
- Custom site-symmetry summary: `m'[x] * m'[y] * 2[z]`.
- Stabilizer size / unitary / antiunitary: `4` / `2` / `2`.
- Recomputed stabilizer matches `single_group_site_symmetry_check.json`: `True`.
- Usable as magnetic local corep input: `True`.
- Unitary subgroup: `Ci` generated by `raw op 5`.
- Antiunitary generator square: raw op `0`.
- Antiunitary conjugation fixes the unitary generator: `True`.
- Wigner case used: `a`.
- Direct code support already present: `False`.
- Dedicated single-group builder sufficient: `True`.
- Full magnetic local corep set successfully built: `True`.
- Constructed magnetic local coreps:
  - `e_g_eta_plus`: dim `1`, source unitary irrep `g`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `e_g_eta_minus`: dim `1`, source unitary irrep `g`, Wigner case `a`, eta `-1`, truly antiunitary `False`.
  - `e_u_eta_plus`: dim `1`, source unitary irrep `u`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `e_u_eta_minus`: dim `1`, source unitary irrep `u`, Wigner case `a`, eta `-1`, truly antiunitary `False`.

### Family `d`

- Representative coordinate: `0, 1/4, 0`.
- Multiplicity: `4`.
- Site symmetry: `2'/m'`.
- Custom site-symmetry summary: `m'[x] * m'[y] * 2[z]`.
- Stabilizer size / unitary / antiunitary: `4` / `2` / `2`.
- Recomputed stabilizer matches `single_group_site_symmetry_check.json`: `True`.
- Usable as magnetic local corep input: `True`.
- Unitary subgroup: `Ci` generated by `raw op 5`.
- Antiunitary generator square: raw op `0`.
- Antiunitary conjugation fixes the unitary generator: `True`.
- Wigner case used: `a`.
- Direct code support already present: `False`.
- Dedicated single-group builder sufficient: `True`.
- Full magnetic local corep set successfully built: `True`.
- Constructed magnetic local coreps:
  - `d_g_eta_plus`: dim `1`, source unitary irrep `g`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `d_g_eta_minus`: dim `1`, source unitary irrep `g`, Wigner case `a`, eta `-1`, truly antiunitary `False`.
  - `d_u_eta_plus`: dim `1`, source unitary irrep `u`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `d_u_eta_minus`: dim `1`, source unitary irrep `u`, Wigner case `a`, eta `-1`, truly antiunitary `False`.

### Family `h`

- Representative coordinate: `1/2, 1/4, 1/4`.
- Multiplicity: `4`.
- Site symmetry: `2/m'`.
- Custom site-symmetry summary: `m'[x] * m'[y] * 2[z]`.
- Stabilizer size / unitary / antiunitary: `4` / `2` / `2`.
- Recomputed stabilizer matches `single_group_site_symmetry_check.json`: `True`.
- Usable as magnetic local corep input: `True`.
- Unitary subgroup: `C2` generated by `raw op 3`.
- Antiunitary generator square: raw op `0`.
- Antiunitary conjugation fixes the unitary generator: `True`.
- Wigner case used: `a`.
- Direct code support already present: `False`.
- Dedicated single-group builder sufficient: `True`.
- Full magnetic local corep set successfully built: `True`.
- Constructed magnetic local coreps:
  - `h_A_eta_plus`: dim `1`, source unitary irrep `A`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `h_A_eta_minus`: dim `1`, source unitary irrep `A`, Wigner case `a`, eta `-1`, truly antiunitary `False`.
  - `h_B_eta_plus`: dim `1`, source unitary irrep `B`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `h_B_eta_minus`: dim `1`, source unitary irrep `B`, Wigner case `a`, eta `-1`, truly antiunitary `False`.

### Family `g`

- Representative coordinate: `0, 1/4, 1/4`.
- Multiplicity: `4`.
- Site symmetry: `2/m'`.
- Custom site-symmetry summary: `m'[x] * m'[y] * 2[z]`.
- Stabilizer size / unitary / antiunitary: `4` / `2` / `2`.
- Recomputed stabilizer matches `single_group_site_symmetry_check.json`: `True`.
- Usable as magnetic local corep input: `True`.
- Unitary subgroup: `C2` generated by `raw op 3`.
- Antiunitary generator square: raw op `0`.
- Antiunitary conjugation fixes the unitary generator: `True`.
- Wigner case used: `a`.
- Direct code support already present: `False`.
- Dedicated single-group builder sufficient: `True`.
- Full magnetic local corep set successfully built: `True`.
- Constructed magnetic local coreps:
  - `g_A_eta_plus`: dim `1`, source unitary irrep `A`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `g_A_eta_minus`: dim `1`, source unitary irrep `A`, Wigner case `a`, eta `-1`, truly antiunitary `False`.
  - `g_B_eta_plus`: dim `1`, source unitary irrep `B`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `g_B_eta_minus`: dim `1`, source unitary irrep `B`, Wigner case `a`, eta `-1`, truly antiunitary `False`.

### Family `b`

- Representative coordinate: `1/2, 0, 0`.
- Multiplicity: `4`.
- Site symmetry: `2'/m`.
- Custom site-symmetry summary: `m'[x] * m'[y] * 2[z]`.
- Stabilizer size / unitary / antiunitary: `4` / `2` / `2`.
- Recomputed stabilizer matches `single_group_site_symmetry_check.json`: `True`.
- Usable as magnetic local corep input: `True`.
- Unitary subgroup: `Cs` generated by `raw op 7`.
- Antiunitary generator square: raw op `0`.
- Antiunitary conjugation fixes the unitary generator: `True`.
- Wigner case used: `a`.
- Direct code support already present: `False`.
- Dedicated single-group builder sufficient: `True`.
- Full magnetic local corep set successfully built: `True`.
- Constructed magnetic local coreps:
  - `b_Ap_eta_plus`: dim `1`, source unitary irrep `A'`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `b_Ap_eta_minus`: dim `1`, source unitary irrep `A'`, Wigner case `a`, eta `-1`, truly antiunitary `False`.
  - `b_App_eta_plus`: dim `1`, source unitary irrep `A''`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `b_App_eta_minus`: dim `1`, source unitary irrep `A''`, Wigner case `a`, eta `-1`, truly antiunitary `False`.

### Family `a`

- Representative coordinate: `0, 0, 0`.
- Multiplicity: `4`.
- Site symmetry: `2'/m`.
- Custom site-symmetry summary: `m'[x] * m'[y] * 2[z]`.
- Stabilizer size / unitary / antiunitary: `4` / `2` / `2`.
- Recomputed stabilizer matches `single_group_site_symmetry_check.json`: `True`.
- Usable as magnetic local corep input: `True`.
- Unitary subgroup: `Cs` generated by `raw op 7`.
- Antiunitary generator square: raw op `0`.
- Antiunitary conjugation fixes the unitary generator: `True`.
- Wigner case used: `a`.
- Direct code support already present: `False`.
- Dedicated single-group builder sufficient: `True`.
- Full magnetic local corep set successfully built: `True`.
- Constructed magnetic local coreps:
  - `a_Ap_eta_plus`: dim `1`, source unitary irrep `A'`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `a_Ap_eta_minus`: dim `1`, source unitary irrep `A'`, Wigner case `a`, eta `-1`, truly antiunitary `False`.
  - `a_App_eta_plus`: dim `1`, source unitary irrep `A''`, Wigner case `a`, eta `+1`, truly antiunitary `False`.
  - `a_App_eta_minus`: dim `1`, source unitary irrep `A''`, Wigner case `a`, eta `-1`, truly antiunitary `False`.

## Outcome

- Successful families: `e, d, h, g, b, a`.
- Failed families: `none`.
- Successful local corep count: `24`.
- New raw point-like magnetic-corep candidate count: `24`.
- Distinct induced unitary vectors among those new candidates: `10`.
- `rank_Z(AI_expanded_v2) = 8` compared with the old `rank_Z(AI_expanded) = 6`.
- Rank-closing example candidates: `e_g_eta_plus, d_g_eta_plus`.
- The point-like antiunitary families close the old rank-2 gap at the rank level, but they still leave a finite-index saturation issue in the BS coefficient lattice.
